jQuery( document ).ready( function() {

    /*
	wp.customize(
		'p_font_family',
		function( value ) {
			value.bind( 
				function( to ) {
					jQuery( 'body.ctypo p' ).css( 'font-family', to );
				} 
			);
		}
	);
    */

} ); // jQuery( document ).ready